import { SearchGuestPipe } from './search-guest.pipe';

describe('SearchGuestPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchGuestPipe();
    expect(pipe).toBeTruthy();
  });
});
